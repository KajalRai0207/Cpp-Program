#include<bits/stdc++.h>
using namespace std;

vector<int>arrayCount(vector<int>n,vector<int>m,vector<int>totalCount){
    vector<vector<int>>res;
    vector<int>current;
    if(current.size()==n[0]){
        res.push_back(current);
        return;
    }
    for(int i=0;i<)

}
int main()
{
    vector<int>n{4};
    vector<int>m{4};
    vector<int>totalCount{2};
    arrayCount(n,m,totalCount);
    return 0;
}